<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.com.xuct.group.purchase.mapper.UserGoodCollectMapper">


    <select id="queryGoodByUserId" resultType="cn.com.xuct.group.purchase.entity.Good">
        select g.id, g.name, g.unit, g.first_drawing, g.start_time, g.end_time, g.inventory
        from bu_good_user_collect c
                 inner join bu_good g on c.good_id = g.id
        where c.user_id = #{userId}
    </select>
</mapper>